module.exports = {
  plugins: [
    /*{
      parserOverride(code, opts) {
        return parse(code.opts);
      }
    },*/
    "babel-plugin-left-side",
  ]
  /*,
  presets: [
    "@babel/preset-env"
  ],
  targets: {
    node: "current"
  }*/
};
